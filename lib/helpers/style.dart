import 'package:flutter/material.dart';

const Color primary = Colors.black;
const red = Colors.red;
const Color white = Colors.white;
const Color black = Colors.black;
const Color grey = Colors.grey;
const Color green = Colors.green;
final Color? active = Colors.redAccent[200];
final Color? disabled = Colors.grey[200];
const Color primaryColor = Color(0xFFFF5252);
InputBorder border = OutlineInputBorder(
  borderSide:
      const BorderSide(color: Color.fromARGB(255, 47, 47, 47), width: 1),
  borderRadius: BorderRadius.circular(0),
);
InputBorder errorBorder = OutlineInputBorder(
  borderSide:
      const BorderSide(color: Color.fromARGB(255, 47, 47, 47), width: 1),
  borderRadius: BorderRadius.circular(0),
);
